# LLM Inspector - Dependencies
# Install: pip install -r requirements.txt

# ── Core ───────────────────────────────────────────────────
mcp>=1.2.0
typer>=0.12.0
rich>=13.7.0
pydantic>=2.7.0
python-dotenv>=1.0.1
httpx>=0.27.0
PyYAML>=6.0.1

# ── LLM Providers (install the ones you need) ─────────────
anthropic>=0.40.0
openai>=1.30.0
google-genai>=1.0.0
# Ollama needs no pip package — just a running Ollama instance

# ── Security Tools ─────────────────────────────────────────
garak>=0.10.0
# Promptfoo is a Node.js tool: npm install -g promptfoo

# ── Development / Testing ─────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.24.0
